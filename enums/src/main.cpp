#include<iostream>  // < > are used to incicate a path on the include directories

enum Example: char
{
    A, B, C
};

int a = 0;
int b = 1;
int c = 2;

int main()
{
    Example value = B;

    if (value == 1)
    {
        //
    }

    std::cin.get();
}